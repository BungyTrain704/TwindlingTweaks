package bungy.twindling.tweaks.main;

import net.minecraft.block.Block;

public class RawAlluminiteBlock extends Block {
    public RawAlluminiteBlock(Settings settings) {
        super(settings);
    }
}
