package bungy.twindling.tweaks.main.block.custom;

import net.minecraft.block.Block;

public class AlluminiteBlock extends Block {
    public AlluminiteBlock(Settings settings) {
        super(settings);
    }
}
